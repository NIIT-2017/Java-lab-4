public interface Heading { //расчет оплаты исходя из руководства (количество подчиненных).

    int calculateHeadingEngineerPayment (int numberEngineer);
    int calculateHeadingManagerPayment (int numberManager);

}
