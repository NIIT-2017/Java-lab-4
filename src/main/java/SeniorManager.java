public class SeniorManager extends ProjectManager {
    SeniorManager(int id, String name, String position, double share, String project, double rate, int emplNum) {
        super(id, name, position, share, project, rate, emplNum);
    }
}
